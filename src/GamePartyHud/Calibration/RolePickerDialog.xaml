<ui:FluentWindow x:Class="GamePartyHud.Calibration.RolePickerDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Change role" Width="320" Height="200"
                 WindowStartupLocation="CenterScreen"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Change role"/>

        <StackPanel Grid.Row="1" Margin="16,0,16,16">
            <TextBlock Text="Role:"/>
            <ComboBox x:Name="Combo" Margin="0,8,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <ui:Button Content="Cancel" Width="90" Margin="0,0,8,0"
                           Appearance="Secondary" IsCancel="True"/>
                <ui:Button Content="Save" Width="90" Icon="Save24"
                           Appearance="Primary" IsDefault="True" Click="OnSave"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
