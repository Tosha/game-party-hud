<UserControl x:Class="GamePartyHud.Hud.MemberCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="240" Height="40"
             Background="Transparent"
             FontFamily="Segoe UI Variable, Segoe UI">
    <Border Padding="6,4" CornerRadius="2" Opacity="{Binding Opacity}"
            BorderBrush="#33FFFFFF" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#66262629" Offset="0"/>
                <GradientStop Color="#661C1C20" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Role glyph tile -->
            <Border Grid.Column="0" Width="26" Height="26" Margin="0,0,8,0"
                    CornerRadius="2"
                    BorderBrush="#55E12A2A" BorderThickness="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#44E12A2A" Offset="0"/>
                        <GradientStop Color="#332A1515" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <TextBlock Text="{Binding RoleGlyph}"
                           Foreground="#FFF2F2F2"
                           FontSize="14"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Nickname}"
                           Foreground="#FFE8E8EA"
                           FontSize="11" FontWeight="SemiBold"
                           TextTrimming="CharacterEllipsis"/>
                <Grid Height="8" Margin="0,3,0,0" Width="90" HorizontalAlignment="Left">
                    <!-- Empty/track -->
                    <Border Background="#661A1A1C" CornerRadius="1"
                            BorderBrush="#44000000" BorderThickness="1"/>
                    <!-- Fill -->
                    <Border CornerRadius="1"
                            HorizontalAlignment="Left"
                            Width="{Binding HpPercent, Converter={StaticResource HpWidthConverter}, ConverterParameter=90}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#FFFF3B3B" Offset="0"/>
                                <GradientStop Color="#FFC81919" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                    <!-- Inner highlight on fill -->
                    <Border CornerRadius="1"
                            HorizontalAlignment="Left"
                            Height="2"
                            VerticalAlignment="Top"
                            Margin="1,1,0,0"
                            Background="#33FFFFFF"
                            Width="{Binding HpPercent, Converter={StaticResource HpWidthConverter}, ConverterParameter=88}"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
