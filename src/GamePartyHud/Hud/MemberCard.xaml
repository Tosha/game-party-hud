<UserControl x:Class="GamePartyHud.Hud.MemberCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="200" Height="24"
             Background="Transparent"
             FontFamily="Segoe UI Variable, Segoe UI">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Border Padding="2,1" CornerRadius="2" Opacity="{Binding Opacity}"
            BorderBrush="#33FFFFFF" BorderThickness="1">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#66262629" Offset="0"/>
                <GradientStop Color="#661C1C20" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Role glyph tile (unchanged from the single-bar layout). -->
            <Border Grid.Column="0" Width="18" Height="18" Margin="0,0,4,0"
                    VerticalAlignment="Center"
                    CornerRadius="2"
                    BorderBrush="{Binding RoleBorderBrush}" BorderThickness="1"
                    Background="{Binding RoleBackgroundBrush}">
                <TextBlock Text="{Binding RoleGlyph}"
                           Foreground="#FFF2F2F2"
                           FontSize="10"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>

            <!-- Bar block: 1-3 stacked bars + hairline dividers + nickname overlay. -->
            <Grid Grid.Column="1" Width="174" Height="22" VerticalAlignment="Center">
                <!-- Outer empty track + border (hosts all bars under one rounded frame). -->
                <Border CornerRadius="2"
                        BorderBrush="#44000000" BorderThickness="1">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#661A1A1C" Offset="0"/>
                            <GradientStop Color="#66111113" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <!-- Stacked rows. Stamina/Mana row heights flip between "*" and "0"
                     from code-behind based on HasStamina/HasMana so visible rows split
                     the area equally and collapsed rows truly contribute zero (a
                     Visibility=Collapsed child does not shrink its parent RowDefinition
                     in WPF — the row itself must drop to 0). -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="HpRowDef" Height="*"/>
                        <RowDefinition x:Name="StaminaRowDef" Height="0"/>
                        <RowDefinition x:Name="ManaRowDef" Height="0"/>
                    </Grid.RowDefinitions>

                    <!-- Row 0: HP (always visible) — darker red palette so the
                         white nickname overlay keeps WCAG-style contrast even
                         when stamina/mana stripes appear below. The nickname
                         lives inside this row (rather than the bar-block-wide
                         overlay) so it's centered on red regardless of how
                         many other bars are stacked underneath. -->
                    <Grid Grid.Row="0">
                        <Border HorizontalAlignment="Left"
                                Width="{Binding HpPercent, Converter={StaticResource HpWidthConverter}, ConverterParameter=174}"
                                Background="{Binding DataContext.HudTheme.HpBarBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        <Border HorizontalAlignment="Left"
                                Height="2" VerticalAlignment="Top"
                                Margin="1,1,0,0"
                                Background="#33FFFFFF"
                                Width="{Binding HpPercent, Converter={StaticResource HpWidthConverter}, ConverterParameter=172}"/>
                        <TextBlock Text="{Binding Nickname}"
                                   Foreground="#FFF2F2F2"
                                   FontSize="11" FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Left"
                                   Margin="6,0,6,0"
                                   TextTrimming="CharacterEllipsis">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="3"
                                                  ShadowDepth="0" Opacity="1.0"/>
                            </TextBlock.Effect>
                        </TextBlock>
                    </Grid>

                    <!-- Row 1: Stamina — themed via HudTheme.StaminaBarBrush. -->
                    <Grid Grid.Row="1" Visibility="{Binding HasStamina, Converter={StaticResource BoolToVis}}">
                        <Border Height="1" VerticalAlignment="Top" Background="#88000000"/>
                        <Border HorizontalAlignment="Left"
                                Margin="0,1,0,0"
                                Width="{Binding StaminaPercent, Converter={StaticResource HpWidthConverter}, ConverterParameter=174}"
                                Background="{Binding DataContext.HudTheme.StaminaBarBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    </Grid>

                    <!-- Row 2: Mana — themed via HudTheme.ManaBarBrush. -->
                    <Grid Grid.Row="2" Visibility="{Binding HasMana, Converter={StaticResource BoolToVis}}">
                        <Border Height="1" VerticalAlignment="Top" Background="#88000000"/>
                        <Border HorizontalAlignment="Left"
                                Margin="0,1,0,0"
                                Width="{Binding ManaPercent, Converter={StaticResource HpWidthConverter}, ConverterParameter=174}"
                                Background="{Binding DataContext.HudTheme.ManaBarBrush, RelativeSource={RelativeSource AncestorType=Window}}"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
