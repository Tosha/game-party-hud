<UserControl x:Class="GamePartyHud.Bars.BarCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Border Padding="10,8"
            CornerRadius="4"
            Background="#0CFFFFFF"
            BorderBrush="#22FFFFFF"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>   <!-- header -->
                <RowDefinition Height="Auto"/>   <!-- content -->
            </Grid.RowDefinitions>

            <!-- Header: bar name (left) + ToggleSwitch (right, only for Stamina/Mana) -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           x:Name="HeaderText"
                           FontSize="13" FontWeight="SemiBold"
                           VerticalAlignment="Center"/>
                <ui:ToggleSwitch Grid.Column="1"
                                 x:Name="EnableToggle"
                                 Visibility="Collapsed"
                                 VerticalAlignment="Center"
                                 Checked="OnEnableToggleChanged"
                                 Unchecked="OnEnableToggleChanged"/>
            </Grid>

            <!-- Content: preview thumbnail (left, 70% of flex) + 30% gap +
                 status icon + pick button (right).
                 The 7*/3* split deliberately leaves whitespace between the
                 preview and the status icon so the bar doesn't read as
                 stretched-too-wide in the card. -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Preview area -->
                <Border Grid.Column="0"
                        x:Name="PreviewBorder"
                        Height="28"
                        Margin="0,0,12,0"
                        CornerRadius="2"
                        Background="#22000000"
                        BorderBrush="#33FFFFFF"
                        BorderThickness="1">
                    <Grid>
                        <Image x:Name="PreviewImage"
                               Stretch="Fill"
                               Visibility="Collapsed"/>
                        <TextBlock x:Name="PreviewPlaceholder"
                                   Text="Pick a region to see a live preview"
                                   FontSize="11"
                                   Opacity="0.5"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Status + button (column 2; column 1 is the deliberate gap) -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusIcon"
                             Width="12" Height="12"
                             Margin="0,0,10,0"
                             Fill="#88FFFFFF"
                             ToolTip="No region picked yet."/>
                    <ui:Button x:Name="PickButton"
                               Appearance="Primary"
                               Icon="Target24"
                               Padding="10,4"
                               Click="OnPickClicked"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
